{
  "name": "youtube-top-comments",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "all": "pnpm install && pnpm lint && pnpm check && pnpm build && pnpm install-test-browser && pnpm test",
    "build": "turbo run build",
    "check": "turbo run check",
    "clean": "turbo daemon clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "start": "turbo run start --parallel",
    "install-test-browser": "turbo run install-test-browser",
    "test": "turbo run test"
  },
  "devDependencies": {
    "prettier": "3.0.3",
    "turbo": "1.10.13"
  },
  "engines": {
    "node": ">=18 < 19"
  },
  "packageManager": "pnpm@8.7.5"
}
